# Laba3-Fadeev-IDB-25-07

**Тема:** Реализация очереди через два стека

**Что сделано:**
1. Написан класс `Queue`, использующий два стека (`std::stack`).
2. Реализованы методы: `enqueue()`, `dequeue()`, `front()`, `printAll()`.
3. Написана тестовая программа в `main()` для проверки работы.

**Результат:**
Программа компилируется без ошибок. Все операции выполняются корректно (принцип FIFO соблюдён). Требования задания выполнены.
